services:
  erp:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DB_PATH: /data/enchante_erp.db
    volumes:
      - erp-data:/data
    restart: unless-stopped

volumes:
  erp-data:
